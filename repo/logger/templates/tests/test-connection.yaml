apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logger.fullname" . }}-test-connection"
  labels:
    {{- include "logger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "logger.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
